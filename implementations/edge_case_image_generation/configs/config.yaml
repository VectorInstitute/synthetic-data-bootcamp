# Root config — swap with hardware=cpu|gpu_l4 and data/source@data=...
defaults:
  - paths: default
  - data/source@data: rdd2022
  - conditioning: default
  - generation: default
  - annotation: default
  - judge: default
  - hardware: cpu
  - _self_

data:
  max_images: 8

output:
  format: coco
