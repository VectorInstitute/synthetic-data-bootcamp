# @package conditioning
depth:
  model_id: depth-anything/Depth-Anything-V2-Base-hf
segmentation:
  # B0 was too blob-like on street scenes; B2 is still CPU-ok and much sharper.
  model_name: nvidia/segformer-b2-finetuned-ade-512-512
  # ADE20K ids: 6=road (used when edit_mask.intersect_seg / seg_intersection).
  ground_class_ids: [6]
